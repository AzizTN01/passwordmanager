import React from 'react';
import {
  View, Text, StyleSheet, SafeAreaView, Image ,TextInput,FlatList, TouchableHighlight, ScrollView
} from 'react-native';



 const Profile =({navigation})=>{
    return(
        <View>

        </View>
    );
 }

 export default Profile